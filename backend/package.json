{
  "name": "oversee-backend",
  "version": "1.0.0",
  "description": "REST API for the backend of oversee app",
  "main": "index.js",
  "scripts": {
    "start": "cross-env NODE_ENV=production node index.js",
    "dev": "cross-env NODE_ENV=development nodemon index.js",
    "test": "cross-env NODE_ENV=test jest --verbose --runInBand"
  },
  "author": "coder-crew",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "express-async-errors": "^3.1.1",
    "mongoose": "^5.12.13"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jest": "^27.0.4",
    "nodemon": "^2.0.7"
  }
}
